import styled from "styled-components";
import TopBar from "./TopBar";

export default function HabitsScreen() {
    
    return (
        <Main>
            <TopBar />
            <p>Preciso ser construído</p>
        </Main>
    );
}

const Main = styled.main`
    width: 100%;
`